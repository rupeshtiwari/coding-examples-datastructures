/**
 * Difficulty Lvel:
 * Problem Link:
 * Definition:
 */
class Solution {
  execute() {}
}

describe('Solution', () => {
  it('Problem #1', () => {
    var start = performance.now();
    expect(new ChocolateProblem().execute()).toEqual();
    var end = performance.now();
    console.log('Problem #1', end - start);
  });
});
